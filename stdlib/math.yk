// Math functionality.

// Returns the absolute value of an integer.
const abs = proc(x: i32) -> i32 {
    if x > 0 { x } else { -x }
};

// Returns the larger of two integers.
const max = proc(x: i32, y: i32) -> i32 {
    if x > y { x } else { y }
};
