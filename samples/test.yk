
const Frac = struct {
    nom: i32;
    den: i32;

    const new = proc(x: i32, y: i32) -> Frac {
        Frac{ nom = x; den = y; }
    };
};

const main = proc() -> i32 {
    var f = Frac.new(2, 654);
    f.den
};
