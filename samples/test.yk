
const Vector2 = proc(T: type) -> type {
	struct {
		x: T;
		y: T;

		const new = proc(x: T, y: T) -> Vector2(T) {
			Vector2(T) { x = x; y = y; }
		};
	}
};

const main = proc() -> i32 {
	//var v = Vector2(i32) { x = 12; y = 34; };
	//v.x
	0
};

