
const Person = struct {
	name: i32;
	age : i32;
}

const List = proc(T: type) -> type {
	struct {
		data  : T  ;
		length: i32;
	}
}

const foo = proc(x: Person) {}
const bar = proc(x: List(Person)) {}

const baz = proc() -> Person {
	Person {
		name = 1233;
		age  = 2345;
	}
}

const main = proc() -> i32 {
	0
}
