
const Vector2 = proc(T: type) -> type {
    struct {
        const Self = Vector2(T);

        x: T;
        y: T;

        const new = proc(x: T, y: T) -> Self {
            Self { x = x; y = y; }
        };
    }
};

const main = proc() -> i32 {
    var v = Vector2(i32).new(12, 56);
    v.y
};

