
const Bar = proc(T: type) -> type {
	struct {
    	value: T;

    	const new = proc(v: T) -> Bar(T) {
        	Bar(T) { value = v; }
    	};
	}
};

const main = proc() -> i32 {
	Bar(i32).new(123).value
};

