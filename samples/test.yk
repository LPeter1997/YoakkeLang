
const Frac = struct {
    nom: i32;
    den: i32;

    const new = proc(n: i32, d: i32) -> Frac {
        Frac{ nom = n; den = d; }
    };
};

const main = proc() -> i32 {
    var a = Frac{ nom = 23; den = 23; nom = 652; };
    0
};
