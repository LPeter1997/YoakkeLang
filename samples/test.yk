
const Frac = struct {
    nom: i32;
    den: i32;

    const new = proc(n: i32, d: i32) -> Frac {
        Frac{ nom = n; den = d; }
    };
};

const Half = Frac.new(1, 2);

const foo = proc() -> i32 {
    1
};

const main = proc() -> i32 {
    0
};
